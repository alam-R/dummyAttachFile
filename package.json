{
  "name": "dummyattachfile",
  "version": "0.0.12",
  "description": "dummy uploading a file for cypress tests",
  "main": "dist/bundle.js",
  "source": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "microbundle build -i src/index.js -f cjs --target node --strict --no-compress --no-generate-types",
    "dev": "microbundle watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alam-R/dummyAttachFile.git"
  },
  "keywords": [
    "cypress",
    "file-upload"
  ],
  "author": "alam",
  "license": "MIT",
  "devDependencies": {
    "microbundle": "^0.13.0"
  }
}
